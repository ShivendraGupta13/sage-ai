{
  "info": {
    "_postman_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "name": "Sage AI",
    "description": "Public Sage Java APIs (`:8080`).\n\n**Prerequisites**\n1. Graph RAG sister service on `:8000` (Neo4j up)\n2. Sage: `./mvnw spring-boot:run`\n3. Optional: Ollama for query interpretation\n\n**SSE tip:** For `POST /ask`, Postman shows the event stream in the response body (`status` → `result` → `done`, or `error`).\n\nContract: `docs/contracts/ask-api.md`",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "baseUrl",
      "value": "http://localhost:8080"
    },
    {
      "key": "correlationId",
      "value": "postman-local-001"
    }
  ],
  "item": [
    {
      "name": "Health",
      "item": [
        {
          "name": "GET /health",
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Accept",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{baseUrl}}/health",
              "host": ["{{baseUrl}}"],
              "path": ["health"]
            },
            "description": "Process health + Graph RAG reachability.\n\n- `200` with `status: \"ok\"` when both retrieve paths are reachable\n- `200` with `status: \"degraded\"` when a reachability flag is false"
          },
          "response": [
            {
              "name": "ok",
              "originalRequest": {
                "method": "GET",
                "header": [
                  {
                    "key": "Accept",
                    "value": "application/json"
                  }
                ],
                "url": {
                  "raw": "{{baseUrl}}/health",
                  "host": ["{{baseUrl}}"],
                  "path": ["health"]
                }
              },
              "status": "OK",
              "code": 200,
              "_postman_previewlanguage": "json",
              "header": [
                {
                  "key": "Content-Type",
                  "value": "application/json"
                }
              ],
              "body": "{\n  \"status\": \"ok\",\n  \"semanticServiceReachable\": true,\n  \"graphServiceReachable\": true\n}"
            }
          ]
        }
      ]
    },
    {
      "name": "Ask",
      "item": [
        {
          "name": "POST /ask — happy path",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Accept",
                "value": "text/event-stream"
              },
              {
                "key": "X-Correlation-ID",
                "value": "{{correlationId}}",
                "description": "Optional. Generated server-side if omitted."
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"query\": \"How did we migrate to OpenTelemetry?\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/ask",
              "host": ["{{baseUrl}}"],
              "path": ["ask"]
            },
            "description": "Streams SSE events:\n1. `status` — Interpreting query…\n2. `status` — Identified problem state and tech context\n3. `status` — Searching knowledge base…\n4. `status` — Ranking results…\n5. `result` — Knowledge Card JSON\n6. `done` — `{}`\n\nOn unrecoverable mid-stream failure: `error` then close (no `done`)."
          },
          "response": []
        },
        {
          "name": "POST /ask — SSRF / email images",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Accept",
                "value": "text/event-stream"
              },
              {
                "key": "X-Correlation-ID",
                "value": "{{correlationId}}"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"query\": \"How did we handle SSRF for external image fetching from emails?\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/ask",
              "host": ["{{baseUrl}}"],
              "path": ["ask"]
            },
            "description": "Alternate seeded-domain style query from architecture examples."
          },
          "response": []
        },
        {
          "name": "POST /ask — gap / out of domain",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Accept",
                "value": "text/event-stream"
              },
              {
                "key": "X-Correlation-ID",
                "value": "{{correlationId}}"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"query\": \"Who invented cold fusion pizza delivery drones?\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/ask",
              "host": ["{{baseUrl}}"],
              "path": ["ask"]
            },
            "description": "Expect Knowledge Card with `gapFlag: true` and no fabricated people/teams when retrieval returns no usable hits."
          },
          "response": []
        },
        {
          "name": "POST /ask — empty query (400)",
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              },
              {
                "key": "Accept",
                "value": "application/json"
              },
              {
                "key": "X-Correlation-ID",
                "value": "{{correlationId}}"
              }
            ],
            "body": {
              "mode": "raw",
              "raw": "{\n  \"query\": \"\"\n}",
              "options": {
                "raw": {
                  "language": "json"
                }
              }
            },
            "url": {
              "raw": "{{baseUrl}}/ask",
              "host": ["{{baseUrl}}"],
              "path": ["ask"]
            },
            "description": "Validation failure before the SSE stream opens. Expect HTTP `400` with shared Error schema (not an SSE `error` event)."
          },
          "response": []
        }
      ]
    }
  ]
}
