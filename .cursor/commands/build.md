Follow .cursor/skills/incremental-implementation/SKILL.md and
.cursor/skills/test-driven-development/SKILL.md.
Work through tasks/plan.md one task at a time: write the test first,
implement, verify it passes, then commit per git-workflow-and-versioning
before moving to the next task.
If $ARGUMENTS contains "auto", complete all tasks without pausing for approval.
Otherwise stop after each task for review.