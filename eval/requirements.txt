# sage-ai evaluation harness dependencies
# Install inside a virtualenv:
#   python -m venv .venv
#   .venv\Scripts\activate        # Windows
#   pip install -r requirements.txt

mlflow>=3.14.0
requests>=2.32.0
numpy>=1.26.0
python-dotenv>=1.0.0
